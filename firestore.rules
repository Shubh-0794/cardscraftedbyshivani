rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Enable full read and write access for development and seamless synchronization
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
